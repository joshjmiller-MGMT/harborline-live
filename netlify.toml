[build]
  publish = "dist"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback so /ensembles/full-band etc. resolve client-side
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
